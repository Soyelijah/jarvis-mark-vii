describe('API Tests', () => {
  test('Servidor debería iniciar correctamente', () => {
    expect(true).toBe(true);
  });

  test('Endpoint raíz debería responder', () => {
    expect(true).toBe(true);
  });
});
